[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --strict-markers
markers =
    unit: fast tests that do not launch StarCraft II
    integration: tests that may launch StarCraft II or touch the live client

